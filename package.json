{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && tsc --project netlify/functions/tsconfig.json",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "netlify-dev": "netlify dev"
  },
  "dependencies": {
    "nuxt": "^3.15.4",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@netlify/functions": "^3.0.1",
    "netlify-cli": "^17.38.1"
  }
}
